# （请填写项目名称）— Agent 导航

> **第一次初始化**：若仓库根目录存在 **`BOOTSTRAP.md`**，请 **优先阅读并按其流程执行**（引导确认项目信息、技术栈与文档填充）。完成并确认内容无误后，删除 `BOOTSTRAP.md`，再视情况精简本段提示。

## 项目概述

**一句话定位：**（请用一两句话说明产品或库是什么、解决什么问题。）

### 目标与范围

- **目标用户 / 场景：**（请填写。）
- **当前阶段：**（例如：原型 / MVP / 持续迭代；可写阶段目标与边界。）
- **非目标（可选）：**（明确不做的范围，避免范围蔓延。）

## 技术栈

请按 **实际仓库** 填写（示例占位，请删除并替换）：

| 类别 | 技术选型 | 备注 |
|------|----------|------|
| 语言 / 运行时 | （如 TypeScript、Python、Go） | 版本： |
| 包管理 / 构建 | （如 pnpm、npm、cargo） |  |
| 前端（如有） | （如框架、UI 库） |  |
| 后端（如有） | （如框架） |  |
| 数据存储 | （如数据库、缓存） |  |
| 其他 | （鉴权、消息队列、可观测性等） |  |

若使用本仓库提供的 **`scripts/check-web-no-emoji.mjs`** 等脚本，请在此说明依赖安装方式（如根目录 `npm install`）及与 pre-commit 的关系。

## 目录结构

请用 **真实目录树** 替换下方示例（可与根目录 `index.md` 保持一致）：

```
（项目根）/
├── （示例：src/ 或 apps/）/
├── harness/          # 若已启用：Agent 任务与记忆
├── scripts/          # 若已从 mmchong-init 应用
└── ...
```

## 快速开始

```bash
# （请填写：安装依赖、启动开发、运行测试等实际命令）
```

## 活跃任务（可选）

若使用 `harness/tasks/`，可说明当前活跃任务入口：例如 [harness/tasks/active/](harness/tasks/active/)。

## 相关链接（可选）

- （架构文档、设计文档、内部 Wiki 等路径）

## 文档维护规则

**核心原则：代码与文档尽量保持一致。**

当修改代码后，建议检查并更新对应文件夹的 **`index.md`**（若项目采用该约定）：

- **同步更新**：该目录用途、核心文件与引用关系是否与实现一致。
- **递归向上**：若影响父级模块描述，同步更新父目录 `index.md`，直至根目录 `index.md`（若适用）。
- **长期记忆**：若有重大架构变更且使用 `harness/memory/MEMORY.md`，请酌情更新。

（若项目 **未** 使用层级 `index.md`，可删除本节或改为团队实际文档流程。）

## 架构与依赖（可选）

若项目有 **分层、模块边界或禁止依赖方向** 规则，请在此说明（示例：领域层不依赖 UI；或列出 `lint` / 脚本如何检查）：

- （待填或删除本节）

## 构建与质量标准

### 常用命令

```bash
# （请填写：build / lint / test / 统一验证 等）
```

### 质量标准（请按团队约定增删）

| 标准 | 规则 |
|------|------|
| 单文件行数 | 建议不超过 500 行（或与 `scripts/verify` 一致） |
| 提交信息 | 建议 [Conventional Commits](https://www.conventionalcommits.org/) |
| 其他 | （如日志、禁止 console、无障碍等） |
